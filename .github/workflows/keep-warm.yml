name: keep-warm

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health
        run: curl -sf -o /dev/null -w "%{http_code}\n" https://resume-builder-api.onrender.com/api/health